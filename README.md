## Azure requirement client example

A client that can be used to connect requirements in tests to corresponding work item in azure.

Usage:
cd requirementsClient
dotnet run <AzureOrgUrl> <PAT> <Pull request Id>

